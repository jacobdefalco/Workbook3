function vendingMachine(coinSlot, selection) {
    if (coinSlot = "Quarter") {
        switch (selection) {
            case "Crunch":
                
                break;
        
            default:
                break;
        }
    }
}


let firstCoin = "Nickel, B5"
let secondCoin = "Quarter, A1"

